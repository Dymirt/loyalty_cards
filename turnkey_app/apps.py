from django.apps import AppConfig


class TurnkeyAppConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "turnkey_app"
    verbose_name = "Legacy marketing-route compatibility"
